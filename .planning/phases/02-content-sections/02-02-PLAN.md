---
phase: 02-content-sections
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/DeviceFrame.tsx
  - src/sections/CaseStudy.tsx
  - src/App.tsx
autonomous: true

must_haves:
  truths:
    - "NomadCrew case study tells a Problem to Solution to Result story"
    - "Case study has a full-width breakout section that feels like its own world"
    - "Architecture diagram shows the tech stack flow visually with CSS/HTML"
    - "Phone mockup device frames display placeholder screenshots inline"
    - "Technical decisions are listed as bullet points alongside the narrative"
  artifacts:
    - path: "src/components/DeviceFrame.tsx"
      provides: "Reusable phone mockup wrapper with CSS bezel"
      exports: ["DeviceFrame"]
    - path: "src/sections/CaseStudy.tsx"
      provides: "NomadCrew case study with full-width breakout"
      exports: ["CaseStudy"]
  key_links:
    - from: "src/sections/CaseStudy.tsx"
      to: "src/data/content.ts"
      via: "import caseStudy"
      pattern: "import.*caseStudy.*from.*data/content"
    - from: "src/sections/CaseStudy.tsx"
      to: "src/components/DeviceFrame.tsx"
      via: "import DeviceFrame"
      pattern: "import.*DeviceFrame.*from.*components/DeviceFrame"
    - from: "src/App.tsx"
      to: "src/sections/CaseStudy.tsx"
      via: "import and render"
      pattern: "import.*CaseStudy.*from.*sections/CaseStudy"
---

<objective>
Build the NomadCrew case study section with full-width breakout layout, CSS device frames, and architecture diagram.

Purpose: The case study is the portfolio's proof of engineering depth. It shows a visitor that Naqeeb doesn't just list skills — he builds real systems with intentional technical decisions.

Output: `src/components/DeviceFrame.tsx` reusable phone mockup, `src/sections/CaseStudy.tsx` full case study section, App.tsx updated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-sections/02-CONTEXT.md
@.planning/phases/02-content-sections/02-RESEARCH.md
@.planning/phases/02-content-sections/02-01-SUMMARY.md
@src/data/content.ts
@src/styles/theme.css
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DeviceFrame component</name>
  <files>src/components/DeviceFrame.tsx</files>
  <action>
Create `src/components/DeviceFrame.tsx` — a pure CSS phone mockup wrapper.

Props:
- `children: React.ReactNode` — content rendered inside the "screen"
- `className?: string` — optional wrapper class for sizing/positioning

Structure:
- Outer wrapper: `relative mx-auto` with `maxWidth: 280px` (adjustable via className)
- Phone bezel: `rounded-[2.5rem] border-[8px] border-bg-dark bg-bg-dark p-1 shadow-xl`
- Notch: `absolute top-0 left-1/2 -translate-x-1/2 w-1/3 h-6 bg-bg-dark rounded-b-2xl z-10`
- Screen area: `rounded-[2rem] overflow-hidden bg-white aspect-[9/19.5]` — the children render here

Use `clsx` for combining className prop. Import from `clsx`.

This component is purely presentational — no state, no effects.
  </action>
  <verify>Run `npx tsc --noEmit` — no type errors</verify>
  <done>DeviceFrame component renders a phone-shaped CSS bezel with notch, accepts children for screen content</done>
</task>

<task type="auto">
  <name>Task 2: Build CaseStudy section and wire into App</name>
  <files>src/sections/CaseStudy.tsx, src/App.tsx</files>
  <action>
Create `src/sections/CaseStudy.tsx` — the NomadCrew deep case study.

Import `caseStudy` from `@/data/content` and `DeviceFrame` from `@/components/DeviceFrame`.

Layout follows the structural full-width breakout pattern (NOT the CSS viewport trick — see research pitfall #1):

```
<section id="work" className="py-section scroll-mt-20">
  {/* 1. Contained intro */}
  <div className="max-w-container mx-auto px-8">
    - Small label: "CASE STUDY" in text-sm uppercase tracking-wide text-text-muted font-heading
    - Project name: "NomadCrew" in text-4xl md:text-5xl font-heading font-bold
    - Problem statement paragraph in font-body text-lg text-text-muted max-w-3xl
  </div>

  {/* 2. Full-width breakout — architecture + screenshots */}
  <div className="w-full bg-bg-feature mt-16 py-16">
    <div className="max-w-container mx-auto px-8">
      - Two-column grid on lg (single column on mobile): grid lg:grid-cols-2 gap-12 items-start

      LEFT column — Architecture diagram:
        - Label: "ARCHITECTURE" in text-sm uppercase tracking-wide text-text-muted
        - Build a simple CSS/HTML flow diagram showing:
          React Native (client) -> Go API -> PostgreSQL (data)
          With WebSockets branching off Go API for real-time
          AWS cloud wrapper around the backend services
        - Use a vertical or horizontal flow with boxes connected by lines
        - Each box: border border-accent/20 rounded-lg px-4 py-3 text-center font-heading text-sm
        - Lines: use border-t or border-l with absolute positioning, OR use simple arrow characters between boxes
        - Keep it simple — boxes stacked vertically with arrow dividers between them works well
        - Below the diagram: 3-4 tech decision bullet points from caseStudy.techDecisions
          Each as: font-body text-base text-text-muted with a small accent-colored dot marker

      RIGHT column — Phone mockups:
        - Two DeviceFrame components side by side (flex gap-6 justify-center)
        - Inside each DeviceFrame: a placeholder div with bg-bg text-text-muted text-center text-sm saying "App Screenshot" and a simple icon from lucide-react (e.g., Smartphone)
        - Below the device frames: app store badge placeholders
          If caseStudy.repoUrl exists, show a link: "View on GitHub" styled as text-accent hover:underline with the ExternalLink icon from lucide-react
    </div>
  </div>

  {/* 3. Contained outro — approach and result */}
  <div className="max-w-container mx-auto px-8 mt-16">
    - "APPROACH" label + approach paragraph
    - Keep it concise — 2-3 sentences max about the collaborative platform built with Go + React Native
  </div>
</section>
```

Update `src/App.tsx`:
- Import `CaseStudy` from `@/sections/CaseStudy`
- Replace the existing `<section id="work">` placeholder with `<CaseStudy />`
- Keep remaining placeholders (experience, contact) as they are

Do NOT add animations — Phase 4.
Do NOT use the CSS `w-[100vw]` viewport trick for full-width — use structural breakout.
  </action>
  <verify>Run `npx vite build` — builds without errors. Run `npx tsc --noEmit` — no type errors. Visually: case study shows contained intro, full-width breakout with architecture boxes and phone frames, then contained outro.</verify>
  <done>CaseStudy renders NomadCrew with problem/approach narrative, CSS architecture diagram, device frame placeholders, tech decision bullets, and GitHub link. Full-width breakout creates visual separation. App.tsx renders CaseStudy in place of the work placeholder.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npx vite build` succeeds
- DeviceFrame renders phone bezel with notch and accepts children
- CaseStudy uses structural breakout (no `100vw` trick)
- Architecture diagram is pure CSS/HTML (no image assets)
- CaseStudy imports from `@/data/content` (not legacy portfolio.js)
- App.tsx renders CaseStudy at the `id="work"` position
- Section has `scroll-mt-20` for navbar offset
</verification>

<success_criteria>
- NomadCrew case study tells a complete Problem -> Solution -> Result story
- Full-width breakout section creates visual distinction from surrounding content
- Architecture diagram communicates the tech stack flow without an image asset
- Phone device frames show placeholders ready for real screenshots
- Technical decisions are displayed as bullet points
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-sections/02-02-SUMMARY.md`
</output>
