---
phase: 02-content-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/content.ts
  - src/sections/HowIBuild.tsx
  - src/App.tsx
autonomous: true

must_haves:
  truths:
    - "Content data is typed and centralized in one file"
    - "How I Build section displays 2-3 bold manifesto-style statements with display typography"
    - "Statements are thoughtful and measured, not aggressive — they communicate engineering philosophy"
  artifacts:
    - path: "src/data/content.ts"
      provides: "All section content as typed constants"
      exports: ["howIBuildStatements", "experiences", "education", "caseStudy", "contactInfo"]
    - path: "src/sections/HowIBuild.tsx"
      provides: "Manifesto-style engineering philosophy section"
      exports: ["HowIBuild"]
  key_links:
    - from: "src/sections/HowIBuild.tsx"
      to: "src/data/content.ts"
      via: "import howIBuildStatements"
      pattern: "import.*howIBuildStatements.*from.*data/content"
    - from: "src/App.tsx"
      to: "src/sections/HowIBuild.tsx"
      via: "import and render"
      pattern: "import.*HowIBuild.*from.*sections/HowIBuild"
---

<objective>
Create the typed content data layer for all Phase 2 sections and build the "How I Build" manifesto section.

Purpose: Establishes the shared data foundation all section components will import from, and delivers the first real content section — bold engineering philosophy statements that set the tone for the entire portfolio.

Output: `src/data/content.ts` with all section data, `src/sections/HowIBuild.tsx` rendering manifesto statements, App.tsx updated to use the new component.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-sections/02-CONTEXT.md
@.planning/phases/02-content-sections/02-RESEARCH.md
@src/styles/theme.css
@src/App.tsx
@src/assets/data/portfolio.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create typed content data file</name>
  <files>src/data/content.ts</files>
  <action>
Create `src/data/content.ts` with typed interfaces and real data extracted from `src/assets/data/portfolio.js`.

Include these exports:

1. `howIBuildStatements` — array of 2-3 manifesto statements. Tone: thoughtful, measured, reflective (NOT aggressive startup bro speak). Example style: "Good architecture is a series of bets on the future." Write 3 statements that communicate how Naqeeb thinks about building software — systems thinking, shipping, and craft.

2. `Experience` interface + `experiences` array — extract from `workExperiences` in portfolio.js:
   - Propwise: Full Stack Engineer, 2025 - Present, one-liner about secure financial APIs
   - Outlier AI: ML Developer (Contract), 2024 - 2025, one-liner about LLM code evaluation
   - Opas Mobile: DevOps Developer, 2023 - 2024, one-liner about CI/CD and cloud infra
   - Crest Data Systems: Software Engineer, 2018 - 2022, one-liner about high-throughput APIs

3. `Education` interface + `education` array:
   - Dalhousie University: Master of Applied Computer Science, 2022 - 2023
   - Charotar University: Bachelor of Technology in IT, 2015 - 2019

4. `CaseStudy` interface + `caseStudy` object for NomadCrew:
   - problem: string (trip coordination pain point)
   - approach: string (collaborative platform with Go + React Native)
   - techDecisions: string[] (3-4 bullet points: Go for concurrency, React Native for cross-platform, PostgreSQL for relational data, WebSockets for real-time)
   - repoUrl: "https://github.com/naqeebali-shamsi/nomadcrew"
   - appStoreUrl/playStoreUrl: null (placeholder — not available yet)
   - screenshots: string[] (empty array — placeholder for future images)

5. `contactInfo` object:
   - email: "naqeebali.shamsi@gmail.com"
   - socials object with github, linkedin, twitter URLs (use "https://x.com/" as twitter placeholder)

Use `as const` where appropriate. All interfaces exported for use by section components.
  </action>
  <verify>Run `npx tsc --noEmit` — no type errors in content.ts</verify>
  <done>content.ts exports all typed data: howIBuildStatements (3 strings), experiences (4 entries), education (2 entries), caseStudy (NomadCrew object), contactInfo (email + 3 social URLs)</done>
</task>

<task type="auto">
  <name>Task 2: Build HowIBuild section and wire into App</name>
  <files>src/sections/HowIBuild.tsx, src/App.tsx</files>
  <action>
Create `src/sections/HowIBuild.tsx`:

- Import `howIBuildStatements` from `@/data/content`
- Render a `<section>` with `id="how-i-build"` and `className="bg-bg-feature py-section scroll-mt-20"`
- Inside: `max-w-container mx-auto px-8`
- Map over statements, rendering each as a `<p>` with:
  - `font-heading text-3xl md:text-4xl lg:text-5xl font-bold leading-tight tracking-tight text-text`
  - `max-w-4xl` to prevent lines from stretching too wide
  - Generous vertical spacing between statements: `space-y-16 md:space-y-24`
- No section heading — the statements ARE the content (manifesto style, not a titled section)
- Export as named export: `export function HowIBuild()`

Update `src/App.tsx`:
- Import `HowIBuild` from `@/sections/HowIBuild`
- Replace the existing `<section id="how-i-build">` placeholder with `<HowIBuild />`
- Keep all other placeholder sections as they are (work, experience, contact) — those get replaced in later plans

Do NOT add any animations or transitions — those are Phase 4 (GSAP).
  </action>
  <verify>Run `npx vite build` — builds without errors. Run `npx tsc --noEmit` — no type errors. Visually: the how-i-build section shows 3 large bold statements with generous spacing on the bg-feature background.</verify>
  <done>HowIBuild section renders 2-3 manifesto statements in display typography. App.tsx imports and renders the component in place of the placeholder. No type errors, no build errors.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npx vite build` succeeds
- `src/data/content.ts` exports all 5 data groups with proper TypeScript interfaces
- `src/sections/HowIBuild.tsx` renders manifesto statements without a section heading
- `src/App.tsx` imports HowIBuild and renders it at the correct position
- The how-i-build section uses `bg-bg-feature` background and `scroll-mt-20` for nav offset
</verification>

<success_criteria>
- Content data file is the single source of truth for all Phase 2 section data
- HowIBuild section displays bold, reflective engineering philosophy statements
- Statements use display-scale typography (text-3xl to text-5xl responsive)
- App renders the real HowIBuild component instead of the placeholder
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-sections/02-01-SUMMARY.md`
</output>
