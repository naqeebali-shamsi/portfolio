---
phase: 02-content-sections
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/hooks/useCopyToClipboard.ts
  - src/components/SocialLinks.tsx
  - src/sections/Experience.tsx
  - src/sections/Contact.tsx
  - src/App.tsx
autonomous: true

must_haves:
  truths:
    - "Experience timeline shows 4 companies with vertical line, dots, company name, role, years, and one-liner"
    - "Education appears as a separate section below the timeline — visually distinct from work experience"
    - "Contact section fills the viewport height with generous whitespace"
    - "Clicking the email copies it to clipboard with visible feedback (text changes to Copied)"
    - "Social links show GitHub, LinkedIn, and Twitter/X as an icon row"
    - "All four Phase 2 sections render in App.tsx in correct order: how-i-build, work, experience, contact"
  artifacts:
    - path: "src/hooks/useCopyToClipboard.ts"
      provides: "Clipboard hook with copied state and auto-reset"
      exports: ["useCopyToClipboard"]
    - path: "src/components/SocialLinks.tsx"
      provides: "Light-theme social icon row"
      exports: ["SocialLinks"]
    - path: "src/sections/Experience.tsx"
      provides: "Timeline + education section"
      exports: ["Experience"]
    - path: "src/sections/Contact.tsx"
      provides: "Full-viewport contact footer"
      exports: ["Contact"]
  key_links:
    - from: "src/sections/Experience.tsx"
      to: "src/data/content.ts"
      via: "import experiences and education"
      pattern: "import.*experiences.*education.*from.*data/content"
    - from: "src/sections/Contact.tsx"
      to: "src/hooks/useCopyToClipboard.ts"
      via: "import useCopyToClipboard"
      pattern: "import.*useCopyToClipboard.*from.*hooks/useCopyToClipboard"
    - from: "src/sections/Contact.tsx"
      to: "src/components/SocialLinks.tsx"
      via: "import SocialLinks"
      pattern: "import.*SocialLinks.*from.*components/SocialLinks"
    - from: "src/App.tsx"
      to: "src/sections/Experience.tsx"
      via: "import and render"
      pattern: "import.*Experience.*from.*sections/Experience"
    - from: "src/App.tsx"
      to: "src/sections/Contact.tsx"
      via: "import and render"
      pattern: "import.*Contact.*from.*sections/Contact"
---

<objective>
Build the experience timeline, contact footer, and wire all Phase 2 sections into App.tsx.

Purpose: Completes the content layer — after this plan, scrolling the site tells a complete story from engineering philosophy through real work to "hire me." The experience timeline shows career progression without resume bloat. The contact section creates a dramatic final screen that drives action.

Output: Experience section with timeline + education, Contact section with click-to-copy email + social links, all sections wired into App.tsx.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-sections/02-CONTEXT.md
@.planning/phases/02-content-sections/02-RESEARCH.md
@.planning/phases/02-content-sections/02-01-SUMMARY.md
@.planning/phases/02-content-sections/02-02-SUMMARY.md
@src/data/content.ts
@src/styles/theme.css
@src/App.tsx
@src/components/ui/social-icons.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared utilities — useCopyToClipboard hook and SocialLinks component</name>
  <files>src/hooks/useCopyToClipboard.ts, src/components/SocialLinks.tsx</files>
  <action>
**useCopyToClipboard hook** (`src/hooks/useCopyToClipboard.ts`):
- `useCopyToClipboard(resetDelay = 2000)` returns `{ copied: boolean, copy: (text: string) => Promise<boolean> }`
- Uses `navigator.clipboard.writeText()` — no fallback library needed
- On success: sets `copied = true`, resets to `false` after `resetDelay` ms via setTimeout
- On failure: logs error, returns false
- Wrap in useCallback with resetDelay dependency

**SocialLinks component** (`src/components/SocialLinks.tsx`):
- Import `contactInfo` from `@/data/content`
- Extract SVG icon paths from the existing `src/components/ui/social-icons.tsx` — copy the exact `<svg>` elements for GitHub, LinkedIn, and X/Twitter (NOT Dribbble)
- Render as a row: `flex items-center gap-6`
- Each link: `<a>` with `text-text-muted hover:text-accent transition-colors` — light theme styling, NOT the dark theme from legacy
- Icon size: `size-5` (20px) — slightly larger than the legacy 18px for better tap targets
- Each link has `target="_blank" rel="noopener noreferrer"` and `aria-label={name}`
- Do NOT import from legacy social-icons.tsx at runtime — copy the SVG markup. The legacy file imports from the dark-themed portfolio.js and uses incompatible styling.
  </action>
  <verify>Run `npx tsc --noEmit` — no type errors</verify>
  <done>useCopyToClipboard returns copied state with auto-reset. SocialLinks renders GitHub, LinkedIn, X icons styled for light theme with hover-to-accent transition.</done>
</task>

<task type="auto">
  <name>Task 2: Build Experience and Contact sections, wire all into App</name>
  <files>src/sections/Experience.tsx, src/sections/Contact.tsx, src/App.tsx</files>
  <action>
**Experience section** (`src/sections/Experience.tsx`):

Import `experiences` and `education` from `@/data/content`.

```
<section id="experience" className="bg-bg-feature py-section scroll-mt-20">
  <div className="max-w-container mx-auto px-8">
```

Timeline heading: "EXPERIENCE" in font-heading text-4xl md:text-5xl uppercase tracking-heading font-bold mb-12.

Vertical timeline structure:
- Outer wrapper: `relative` with the vertical line as a pseudo-element or a `<div>`
- Vertical line: `absolute left-3 top-2 bottom-2 w-px bg-accent/30` (subtle teal line)
- Each entry wrapper: `relative pl-10` with `space-y-10` between entries
- Dot for each entry: `absolute left-1.5 top-2 w-3 h-3 rounded-full bg-accent border-2 border-bg-feature` — sits on the line
- Entry content:
  - Years: `font-body text-sm text-text-muted`
  - Company: `font-heading text-xl font-bold text-text mt-1`
  - Role: `font-body text-base text-text-muted`
  - One-liner: `font-body text-sm text-text-muted mt-1`

Education sub-section (below timeline, visually distinct):
- Divider: `mt-16 pt-12 border-t border-accent/10`
- Heading: "EDUCATION" in font-heading text-2xl uppercase tracking-heading font-bold mb-8
- Render education entries WITHOUT the timeline dots/line — just a simple list
- Each entry: school name (font-heading text-lg font-bold), degree (font-body text-base text-text-muted), years (font-body text-sm text-text-muted)
- Two entries side by side on md+ (`grid md:grid-cols-2 gap-8`), stacked on mobile

**Contact section** (`src/sections/Contact.tsx`):

NOTE: A legacy `src/sections/Contact.tsx` exists — OVERWRITE it entirely. It uses TerminalText and dark theme styling which are incompatible.

Import `contactInfo` from `@/data/content`, `useCopyToClipboard` from `@/hooks/useCopyToClipboard`, and `SocialLinks` from `@/components/SocialLinks`.

```
<section id="contact" className="min-h-screen flex flex-col items-center justify-center py-section px-8 scroll-mt-20">
```

Layout (centered, generous whitespace):
- Small label: "GET IN TOUCH" in font-heading text-sm uppercase tracking-wide text-text-muted mb-8
- Email as the hero element: a `<button>` with `onClick={() => copy(contactInfo.email)}`
  - Default state: show the email address in `font-heading text-2xl md:text-4xl lg:text-5xl font-bold tracking-tight text-text hover:text-accent transition-colors cursor-pointer`
  - Copied state: show "Copied!" in `text-accent` (same size/weight)
  - Below the button: small helper text "Click to copy" in `text-sm text-text-muted mt-2` — hides when copied (shows "Copied to clipboard" instead)
- SocialLinks component: rendered below email with `mt-12`
- The min-h-screen + flex centering creates the full-viewport dramatic feel

**App.tsx update:**

Import `Experience` from `@/sections/Experience` and `Contact` from `@/sections/Contact`.

Replace the remaining two placeholder sections:
- `<section id="experience">` placeholder -> `<Experience />`
- `<section id="contact">` placeholder -> `<Contact />`

After this, App.tsx should import and render all 4 section components in order:
1. Hero placeholder (unchanged — Phase 3)
2. `<HowIBuild />`
3. `<CaseStudy />`
4. `<Experience />`
5. `<Contact />`

Verify the section order matches the nav link order in Navbar.
  </action>
  <verify>
Run `npx vite build` — builds without errors.
Run `npx tsc --noEmit` — no type errors.
Visually confirm:
- Experience section shows 4 entries with a vertical line and dots
- Education appears below with 2 degrees in a grid
- Contact section fills viewport, email is large and clickable
- Clicking email changes text to "Copied!" briefly
- Social icons render below email with correct hover color
  </verify>
  <done>
Experience timeline renders 4 work entries with vertical line/dots and education sub-section.
Contact section fills viewport with click-to-copy email and social icon row.
App.tsx renders all 4 Phase 2 sections in order — no placeholder sections remain except the hero.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npx vite build` succeeds
- Experience timeline: vertical line with 4 dots/entries, education grid below
- Contact: full-viewport, email click triggers clipboard copy with "Copied!" feedback
- SocialLinks: 3 icons (GitHub, LinkedIn, X) with light-theme styling
- App.tsx: all 4 section components imported and rendered in correct order
- All sections have `scroll-mt-20` for navbar offset
- No Framer Motion imports anywhere (GSAP-only decision)
- No dark theme classes (bg-neutral-*, text-neutral-*) — uses design token classes only
- Legacy Contact.tsx is fully replaced (no TerminalText import)
</verification>

<success_criteria>
- Scrolling the site tells a complete story: philosophy -> work -> experience -> contact
- Experience timeline is minimal (no resume dump — company, role, years, one-liner only)
- Education is visually separate from work experience
- Contact section creates urgency with full-viewport dramatic layout
- Email interaction works: click copies to clipboard with visible confirmation
- All nav links scroll to their correct sections with proper offset
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-sections/02-03-SUMMARY.md`
</output>
