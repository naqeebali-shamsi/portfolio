---
phase: 06-ship
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - src/App.tsx
  - public/og-image.png
  - vercel.json
autonomous: true

must_haves:
  truths:
    - "Page has a compelling title and meta description optimized for search"
    - "Open Graph tags are present so link previews show name, title, and OG image on social platforms"
    - "Twitter Card tags are present for X/Twitter link previews"
    - "JSON-LD Person schema is embedded and valid"
    - "Canonical URL points to https://naqeebali.me"
    - "A static OG image placeholder exists at /og-image.png (1200x630)"
    - "Vercel config file documents deployment settings"
    - "Semantic HTML landmarks are correct (main, nav, section, footer)"
  artifacts:
    - path: "index.html"
      provides: "Complete SEO meta tags, OG tags, Twitter Card, JSON-LD, canonical"
      contains: "application/ld+json"
    - path: "public/og-image.png"
      provides: "Static 1200x630 OG image (must be > 1KB)"
    - path: "vercel.json"
      provides: "Deployment configuration for Vercel"
      contains: "rewrites"
    - path: "src/App.tsx"
      provides: "Semantic footer landmark"
      contains: "footer"
  key_links:
    - from: "index.html"
      to: "public/og-image.png"
      via: "og:image meta tag"
      pattern: "og:image"
    - from: "index.html"
      to: "https://naqeebali.me"
      via: "canonical link"
      pattern: "canonical"
---

<objective>
Add SEO markup to index.html and prepare Vercel deployment configuration.

Purpose: The site must be findable by search engines, show rich link previews on social platforms, and have deployment config ready for Vercel. No actual deployment -- localhost only per user decision.
Output: Complete SEO markup in index.html, static OG image placeholder, and vercel.json config file.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ship/06-CONTEXT.md
@.planning/phases/06-ship/06-RESEARCH.md

Key files:
@index.html
@src/App.tsx
@src/data/content.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: SEO markup in index.html and semantic HTML audit</name>
  <files>
    index.html
    src/App.tsx
  </files>
  <action>
**Update index.html with complete SEO markup:**

Replace the current minimal head content with:

1. **Title:** `Naqeebali Shamsi -- Cloud Developer, Solution Architect, Full Stack Engineer` (or similar -- optimize for search ranking and social sharing, Claude's discretion on exact format)

2. **Meta description:** Write compelling copy in the spirit of the user's voice -- rotating roles + "I make tech work harder so you don't have to." Example: "Cloud Developer, Solution Architect, and Full Stack Engineer. I build systems that scale -- from infrastructure to interface. Explore my work and engineering philosophy." Claude's discretion on exact wording.

3. **Open Graph tags:**
```html
<meta property="og:type" content="website" />
<meta property="og:title" content="Naqeebali Shamsi | Full Stack Developer" />
<meta property="og:description" content="[same as meta description]" />
<meta property="og:image" content="https://naqeebali.me/og-image.png" />
<meta property="og:url" content="https://naqeebali.me" />
<meta property="og:site_name" content="Naqeebali Shamsi" />
```

4. **Twitter Card tags:**
```html
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content="[same as og:title]" />
<meta name="twitter:description" content="[same as meta description]" />
<meta name="twitter:image" content="https://naqeebali.me/og-image.png" />
```

5. **JSON-LD Person schema** (in a `<script type="application/ld+json">` tag):
```json
{
  "@context": "https://schema.org",
  "@type": "Person",
  "@id": "https://naqeebali.me/#person",
  "name": "Naqeebali Shamsi",
  "url": "https://naqeebali.me",
  "jobTitle": "Full Stack Developer",
  "knowsAbout": ["React", "Node.js", "TypeScript", "Cloud Architecture", "Go", "AWS"],
  "sameAs": [
    "https://github.com/naqeebali-shamsi",
    "https://linkedin.com/in/naqeebali-shamsi"
  ]
}
```
Note: Use placeholder URLs for sameAs -- user will fill in exact profile URLs. Claude's discretion on schema depth (knowsAbout items, additional properties).

6. **Canonical URL:**
```html
<link rel="canonical" href="https://naqeebali.me" />
```

7. Remove the `keywords` meta tag -- it's been ignored by Google since 2009 and adds noise.

**Semantic HTML audit in App.tsx:**
- Verify `<main>` wraps content (already does)
- Verify `<nav>` is used for navigation (already in Navbar.tsx)
- Verify each content area uses `<section>` with an id (already done)
- Add a minimal `<footer>` element after the Contact section in App.tsx (e.g., `<footer className="text-center text-sm text-stone-400 py-4">&copy; 2026 Naqeebali Shamsi</footer>`). Do NOT change Contact's outer tag — keep it as `<section>` to preserve scroll animations and section targeting.
- Verify heading hierarchy: single `<h1>` in HeroText.tsx, `<h2>` for section headings. Check that no heading levels are skipped.

Do NOT add `<footer>` to the existing sections list if it would break scroll animations or section targeting. A minimal semantic footer after all sections is fine.
  </action>
  <verify>
Run `npx vite build` -- build succeeds. Open the built `dist/index.html` and verify: title tag is correct, meta description present, all OG tags present, Twitter Card tags present, JSON-LD script block present, canonical link present. Validate JSON-LD is valid JSON (parse it). Verify `<footer` is present in App.tsx output.
  </verify>
  <done>
index.html has complete SEO markup: optimized title, compelling meta description, OG tags, Twitter Card tags, valid JSON-LD Person schema, and canonical URL. Semantic HTML uses proper landmarks. No keywords meta tag.
  </done>
</task>

<task type="auto">
  <name>Task 2: OG image placeholder and Vercel deployment config</name>
  <files>
    public/og-image.png
    vercel.json
  </files>
  <action>
**Static OG image using puppeteer (already in devDependencies):**
Create a script `scripts/generate-og.mjs` that uses puppeteer to:
1. Render a simple HTML card (1200x630px viewport) with:
   - Background color: #FAFAF5 (brand bg color)
   - "NAQEEBALI SHAMSI" in Space Grotesk 700, large, dark (#32292F)
   - "Full Stack Developer | Cloud Architect" subtitle in lighter color (#705D56)
   - Clean, minimal, premium feel -- matches the site's design language
2. Screenshot to `public/og-image.png` at exactly 1200x630px
3. Close browser

Run `node scripts/generate-og.mjs` to generate the image. Verify the output file exists and is > 1KB (not a zero-byte file).

**Vercel deployment config:**
Create `vercel.json` in project root:
```json
{
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }
      ]
    }
  ]
}
```

This documents the deployment settings. The user's domain (naqeebali.me) is already configured on Vercel from their current site.
  </action>
  <verify>
Verify `public/og-image.png` exists, is > 1KB (not zero-byte), and is approximately 1200x630px. Verify `vercel.json` is valid JSON (parse it). Run `npx vite build` to confirm vercel.json doesn't interfere with the build.
  </verify>
  <done>
Static OG image placeholder exists at public/og-image.png (1200x630). vercel.json is created with correct Vite deployment settings, SPA rewrites, and asset caching headers.
  </done>
</task>

</tasks>

<verification>
1. `npx vite build` succeeds
2. `dist/index.html` contains: title, meta description, og:title, og:description, og:image, twitter:card, JSON-LD script, canonical link
3. No `keywords` meta tag present
4. JSON-LD in dist/index.html is valid JSON
5. `public/og-image.png` exists at correct dimensions
6. `vercel.json` is valid JSON with correct settings
7. Heading hierarchy is correct (single h1, sequential h2/h3)
</verification>

<success_criteria>
- Complete SEO meta tags in index.html (title, description, OG, Twitter Card, JSON-LD, canonical)
- Valid JSON-LD Person schema
- Static OG image at public/og-image.png (1200x630)
- vercel.json with Vite deployment config
- Semantic HTML landmarks verified
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/06-ship/06-02-SUMMARY.md`
</output>
